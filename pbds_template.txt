
#include <ext/pb_ds/assoc_container.hpp>
#include <ext/pb_ds/tree_policy.hpp>
using namespace std;
using namespace __gnu_pbds;

template <class T>
using ordered_set = tree<
    T,
    null_type,
    less<T>,
    rb_tree_tag,
    tree_order_statistics_node_update
>;

// ordered_set<int> st;

// st.insert(5);          // insert element
//st.erase(5);           // erase element

//st.order_of_key(5);    // count of elements strictly smaller than 5

//st.find_by_order(0);   // iterator to 0th smallest element
                       // (0-based indexing)